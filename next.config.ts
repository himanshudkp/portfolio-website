import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "mir-s3-cdn-cf.behance.net",
      "bigpicture.stage.appfire.com",
      "www.hostinger.com",
    ],
  },
};

export default nextConfig;
